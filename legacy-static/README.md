# Legacy Static Website (Archived)

These HTML files are the previous multi-page static site from early GitHub commits.

The **active platform** is the Next.js LMS in the project root.

To run the LMS:

```bash
npm install
npm run db:setup
npm run dev
```

Open http://localhost:3000
