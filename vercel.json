{
  "buildCommand": "node scripts/prepare-vercel-build.mjs && prisma generate && prisma db push --skip-generate && tsx prisma/seed.ts && next build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "regions": ["iad1"]
}
