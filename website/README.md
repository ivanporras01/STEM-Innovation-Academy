# Legacy Static Website

This folder contains the original Phase 1 marketing landing page (HTML/CSS/JS).

The active platform is now the **Next.js LMS** in the project root. Run:

```bash
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000)

The static site in this folder is kept for reference only.
